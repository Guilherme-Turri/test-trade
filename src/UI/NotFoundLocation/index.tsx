import { Map } from '@styled-icons/foundation/Map'
import * as Styled from './styles'

export const NotFoundLocation = () => {
  return (
    <Styled.Img data-testid='NotFoundLocation'>
      <Map />
    </Styled.Img>
  )
}