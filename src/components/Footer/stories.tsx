import { Footer } from ".";

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  name: 'Footer',
  component: Footer,
}

export const Template = (args: any) => {
  return (
    <div>
      <Footer />
    </div>
  )
}
